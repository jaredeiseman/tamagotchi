import React from 'react';

function ProgressBar(props) {
  return (
    <div style={{width: props.barWidth + '%', height: '100%', background: '#0b0'}}>

    </div>
  );
}

export default ProgressBar;
